import * as React from "react";
import {Component} from "react";
import {CodeBlock} from "../../components/codeBlock/codeBlock";

export class BubbleSortListinigJS extends Component<{}, undefined> {

    render() {
        return (
            <div>
                <CodeBlock
                    ident={'comparison'}
                    text={'ttest'}>
                    <CodeBlock
                        ident={'permutation.temp'}
                        text={'ttest2'}/>
                </CodeBlock>
            </div>
        );
    }
}